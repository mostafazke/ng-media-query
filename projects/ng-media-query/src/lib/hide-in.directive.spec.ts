import { HideInDirective } from './hide-in.directive';

describe('HideInDirective', () => {
  it('should create an instance', () => {
    const directive = new HideInDirective();
    expect(directive).toBeTruthy();
  });
});
