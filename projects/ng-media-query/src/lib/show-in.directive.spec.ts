import { ShowInDirective } from './show-in.directive';

describe('ShowInDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowInDirective();
    expect(directive).toBeTruthy();
  });
});
